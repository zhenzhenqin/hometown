# 🌏 衢州地区信息管理系统 (Quzhou Hometown System)

> **南孔圣地 · 衢州有礼**
> 基于 Spring Boot 3 + Vue 3 的全栈前后端分离项目。旨在通过数字化手段展示衢州文化、旅游与特产资源，并提供基于 ECharts 的数据驾驶舱与完善的后台管理功能。

![Java](https://img.shields.io/badge/Java-17%2B-green) ![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.x-green) ![Vue](https://img.shields.io/badge/Vue-3.x-42b883) ![MySQL](https://img.shields.io/badge/MySQL-8.0-blue) ![Redis](https://img.shields.io/badge/Redis-Latest-red)

---

## 📖 项目简介

本项目是一个集**前台沉浸式展示**与**后台高效管理**于一体的高性能全栈系统。采用业界主流的 **Spring Boot 3 + Vue 3** 前后端分离架构。

系统不仅在**“读”**层面利用缓存极大提升了页面加载速度，更在**“写”**层面利用 **Redis** 承载了高频的点赞与交互流量，巧妙平衡了用户体验与数据一致性，是应对高并发场景的典型实践。

### ✨ 核心亮点

* **全栈架构**：采用前沿的 Spring Boot 3 + Vue 3 (Setup) + Vite 技术栈，结构清晰。
* **Redis 读写双重加速**：
    * **读缓存（热点数据）**：对首页轮播图、热门景点排行进行缓存预热，系统响应速度提升 **50%+**。
    * **写缓冲（高并发点赞）**：利用 Redis 的 `Set` 集合结构存储点赞状态，利用其**原子性**实现秒级去重与快速计数，配合定时任务将数据**异步同步**至 MySQL，完美解决高并发下的数据库写入瓶颈。
* **数据可视化**：后台集成 **ECharts** 实现数据驾驶舱，实时监控景点热度与用户交互趋势。
* **安全风控体系**：
    * **认证鉴权**：基于 JWT 的无状态单点登录。
    * **防刷机制**：集成 Kaptcha 图形验证码。
    * **日志审计**：AOP 切面自动记录敏感操作，实现全链路追踪。
* **云端能力**：接入 **阿里云 OSS**，实现海量图片资源的高效存储与 CDN 分发。

---

## 🧩 功能模块详情

### 1. 业务交互模块
* **景点/特产/文化管理**: 支持富文本编辑、图片上传 (OSS)。
* **高性能交互**:
    * **点赞/收藏**: 采用 Redis 缓存点赞状态，支持用户极速点赞与取消，后台异步持久化。
    * **浏览**: 瀑布流展示，支持按热度（Redis 统计）排序。
* **用户中心**: 个人信息修改、密码重置、交互历史查看。

### 2. 后台管理系统
* **📊 数据驾驶舱**:
    * **核心指标**: 用户总量、景点收录数、**全站总点赞数**（实时聚合）。
    * **图表分析**: 用户增长趋势图、特产价格分布图、景点人气排行榜。
* **🛡️ 系统安全**:
    * **用户管理**: 账号禁用/启用，风险账号管控。
    * **操作日志**: 记录管理员的每一次增删改操作。

---

## 🚀 快速开始 (Quick Start)

请按照以下步骤部署并运行项目。

### 0. 环境准备 (Prerequisites)
确保您的开发环境已安装以下软件：
* **Java**: JDK 17 或更高版本
* **Node.js**: 16.0 或更高版本 (推荐 LTS)
* **Database**: MySQL 8.0+
* **Cache**: Redis 5.0+
* **Maven**: 3.6+

### 1. 数据库初始化 (Database Setup)
1.  登录 MySQL 数据库：
    ```bash
    mysql -u root -p
    ```
2.  创建数据库（建议库名为 `hometown_db`）：
    ```sql
    CREATE DATABASE hometown_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
    USE hometown_db;
    ```
3.  导入初始化脚本：
    * 脚本路径：`hometown/qu-server/src/main/resources/init.sql`
    * 执行导入（或使用 Navicat/DBeaver 工具导入）：
        ```sql
        SOURCE /your/project/path/hometown/qu-server/src/main/resources/init.sql;
        ```

### 2. 后端配置与启动 (Backend)
1.  **修改配置文件**：
    打开 `hometown/qu-server/src/main/resources/application.yml`，根据您的环境修改以下配置：
    * **MySQL**: 修改 `spring.datasource.username` 和 `password`。
    * **Redis**: 修改 `spring.data.redis.host` 和 `port` (如有密码需填写)。
    * **Aliyun OSS**: 填入您的 `accessKeyId`, `accessKeySecret`, `bucketName`, `endpoint` (用于图片上传，如暂无Key可跳过，但上传功能将不可用)。

2.  **启动服务**：
    进入后端根目录并运行：
    ```bash
    cd hometown
    mvn clean package -DskipTests
    java -jar qu-server/target/qu-server-1.0.0.jar
    # 或者直接在 IDEA 中运行 HometownApplication.java
    ```
    > 💡 **提示**: 后端服务默认启动在 `8080` 端口。API 文档地址: `http://localhost:8080/doc.html`

### 3. 后台管理端启动 (Admin Frontend)
1.  进入管理端项目目录：
    ```bash
    cd vue_hometown_management
    ```
2.  安装依赖并启动：
    ```bash
    npm install
    npm run dev
    ```
3.  **访问地址**: `http://localhost:5173`
    * **默认管理员账号**: `admin`
    * **默认密码**: `123456` (请参考 init.sql 中的具体数据)

### 4. 用户展示端启动 (User Frontend)
1.  进入展示端项目目录：
    ```bash
    cd vue_showhometown
    ```
2.  安装依赖并启动：
    ```bash
    npm install
    npm run dev
    ```
3.  **访问地址**: `http://localhost:5174` (若5173被占用，Vite会自动递增端口)

---

## 🛠️ 技术栈 (Tech Stack)

| 模块 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| **后端** | **Spring Boot 3.x** | 核心框架 |
| | MyBatis-Plus | ORM 框架，简化 SQL 操作 |
| | Redis | 缓存中间件 (热点数据缓存) |
| | JWT + Interceptor | 用户鉴权与登录拦截 |
| | AOP (AspectJ) | 操作日志记录、公共字段填充 |
| | Aliyun OSS | 对象存储服务 |
| **管理端** | **Vue 3** | Composition API 风格 |
| | Element Plus | 桌面端 UI 组件库 |
| | ECharts 5 | 数据可视化图表 |
| | Vite | 下一代前端构建工具 |
| **展示端** | **Vue 3** | 响应式布局设计 |
| | Axios | HTTP 请求客户端 |
| | CSS3 / SCSS | 页面样式与动画 |

---

## 📂 工程结构 (Structure)

```plaintext
root
├── hometown (后端工程)
│   ├── qu-common      # 公共模块 (Result, Utils, Constant, Exception)
│   ├── qu-pojo        # 实体模块 (Entity, DTO, VO)
│   └── qu-server      # 业务核心 (Controller, Service, Mapper, Aspect)
│
├── vue_hometown_management (后台管理端)
│   ├── src/api        # API 接口统一管理
│   ├── src/views      # 页面视图 (Dashboard, User, Log, etc.)
│   └── src/layout     # 侧边栏与顶部布局
│
└── vue_showhometown (用户展示端)
    ├── src/views      # 沉浸式展示页面
    └── src/components # 公共组件